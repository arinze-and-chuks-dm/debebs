backend:
  name: github
  repo: arinze-and-chuks-dm/debebs
  branch: main
  squash_merges: true
  open_authoring: true

publish_mode: editorial_workflow
site_url: 'https://www.debebsmedia.com/' # your site name
media_folder: 'src/assets/images' # Location where files will be stored in the repo
public_folder: 'src/assets/images' # The src attribute for uploaded media

collections:
  - name: 'blog' # Used in routes, e.g., /admin/collections/blog
    label: 'Blog' # Used in the UI
    folder: 'src/content/blog' # The path to the folder where the documents are stored
    format: frontmatter
    preview_path: blog/{{slug}}
    create: true # Allow users to create new documents in this collection
    fields: # The fields for each document, usually in frontmatter
      - { label: 'Layout', name: 'layout', widget: 'hidden', default: 'blog' }
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'Description', name: 'description', widget: 'string' }
      - { label: 'Publish Date', name: 'publish date', widget: 'datetime' }
      - { label: 'Updated Date', name: 'updated date', widget: 'datetime' }
      - label: 'Cover'
        name: 'cover'
        widget: 'object'
        collapsed: true
        fields:
          - { label: 'Image', name: 'image', widget: 'image' }
          - { label: 'Alt', name: 'alt', widget: 'string' }
# https://github.dev/gxanshu/astro-decap-cms-starter
